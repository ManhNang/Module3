Giải thích sự khác biệt giữa INNER JOIN và LEFT JOIN trong SQL bằng biểu đồ Venn và lý thuyết tập hợp?

Kỹ thuật LEFT JOIN kết hợp WHERE IS NULL (Anti-Join) hoạt động như thế nào để lọc các bản ghi mồ côi chưa từng phát sinh giao dịch?

Tại sao nên sử dụng COUNT(column_b) thay vì COUNT(\*) khi kết hợp LEFT JOIN với GROUP BY để tránh đếm sai các bản ghi NULL?

Khi nào việc sử dụng LEFT JOIN là bắt buộc để đảm bảo giữ nguyên toàn vẹn số lượng bản ghi của bảng gốc trong báo cáo?

Việc đánh chỉ mục (INDEX) trên các cột tham gia điều kiện ON ảnh hưởng thế nào đến tốc độ xử lý của INNER JOIN và LEFT JOIN?
